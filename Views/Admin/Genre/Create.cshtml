@model GenreCreateVM
@{
    ViewData["Title"] = "Create Genre";
    ViewData["PageDescription"] = "Add a new Genre";
    string siteUrl = Context.Request.Scheme + System.Uri.SchemeDelimiter + Context.Request.Host.Value;
}

@section css
{
<link href="~/plugins/select2/dist/css/select2.min.css" rel="stylesheet" />
<link href="~/assets/plugins/bootstrap-wysihtml5/bootstrap3-wysihtml5.min.css" rel="stylesheet" />
}

@section breadcrumb
{
<li><a asp-controller="Admin" asp-action="Index"><i class="fa fa-dashboard"></i> Admin</a></li>
<li><a asp-controller="Genre" asp-action="Index"><i class="fa fa-users"></i> Genres</a></li>
<li class="active"><i class="fa fa-user"></i> Create Genre</li>
}


<div class="row">
    <div class="col-xs-12">
        <div class="box box-primary">
            <div class="box-header with-border">
                <h3 class="box-title">@ViewData["PageDescription"]</h3>
                <div class="box-tools pull-right">
                    <button type="button" class="btn btn-box-tool" data-widget="collapse" data-toggle="tooltip" title="Collapse">
                        <i class="fa fa-minus"></i>
                    </button>
                    <button type="button" class="btn btn-box-tool" data-widget="remove" data-toggle="tooltip" title="Remove">
                        <i class="fa fa-times"></i>
                    </button>
                </div>
            </div>
            <form class="form-horizontal" asp-controller="Genre" asp-action="Create">
                <div class="box-body">
                    <div asp-validation-summary="All" class="text-danger"></div>

                    <div class="form-group m-t-20">
                        <label asp-for="@Model.Name" class="col-sm-2 control-label">Name: *</label>
                        <div class="col-sm-9">
                            <input class="form-control" placeholder="Name" asp-for="@Model.Name" >
                            <span asp-validation-for="@Model.Name" class="text-danger"></span>
                        </div>
                    </div>
                    <div class="form-group">
                        <label asp-for="@Model.Info" class="col-sm-2 control-label">Info: *</label>
                        <div class="col-sm-9">
                            <textarea class="textarea" placeholder="Place some text here" style="width: 100%; height: 200px; font-size: 14px; line-height: 18px; border: 1px solid #dddddd; padding: 10px;" asp-for="@Model.Info" required></textarea>
                            <span asp-validation-for="@Model.Info" class="text-danger"></span>
                        </div>
                    </div>
                </div>
                <div class="box-footer">
                    <button onclick="goBack()" class="btn btn-danger">Cancel</button>
                    <button type="submit" class="btn btn-success">&nbsp;Save&nbsp;</button>
                </div>
            </form>
        </div>
    </div>
</div>

@section scripts
{
<script src="~/plugins/select2/dist/js/select2.full.min.js"></script>
<script src="~/assets/plugins/bootstrap-wysihtml5/bootstrap3-wysihtml5.all.min.js"></script>
}

@section scr
{
<script>
    $(function () {
        $('.textarea').wysihtml5()
    })
</script>
}